# Copy to .env (gitignored) before running docker compose up.
POSTGRES_USER=taskapi
POSTGRES_PASSWORD=taskapi
POSTGRES_DB=tasks

# What the app container uses to connect. "db" is the Postgres service name below,
# resolved inside the compose network — not localhost.
DATABASE_URL=postgresql://taskapi:taskapi@db:5432/tasks
